<% if (site.tags.length){ %>
<div class="list-group">
  <% site.tags.sort('name').each(function(item){ %>
    <a class="list-group-item" href="<%- config.root + item.path %>"><%= item.name %><span class="badge"><%= item.posts.length %></span></a>
  <% }); %>
</div>
<% } %>