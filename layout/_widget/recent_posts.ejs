<% if (site.posts.length){ %>
<ul class="list-group">
<% site.posts.sort('date', -1).limit(5).each(function(post){ -%>
  <li class="list-group-item icon"><a href="<%- config.root + post.path %>"><%= post.title %></a></li>
<% }); -%>
</ul>
<% } %>
