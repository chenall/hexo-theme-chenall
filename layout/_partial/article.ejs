<%partial('module'); -%>
<%
var contents = partial('contents',{__: hexo.__('post')});
if (iTags && iTags.length)
{
  iTags.forEach(function(iTag){
    contents = contents.replace(iTag.preg,iTag.func);
  })
}; -%>
<%- contents -%>
